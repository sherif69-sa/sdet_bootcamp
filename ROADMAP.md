# Roadmap

## v0.3.0
- Add a "doctor" command to validate env + dependencies quickly.
- Add a tiny local HTTP test server utility for API testing exercises.
- Add more SDET-focused exercises (katas) with clear inputs/outputs.

## v0.4.0
- Add tracing/observability helpers (request-id propagation, timing summaries).
- Add a small fixture library for tests (tmp dirs, env isolation, subprocess helpers).
