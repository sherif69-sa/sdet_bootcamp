# Day 26 external contribution push sample

```bash
python -m sdetkit external-contribution-push --format json --strict
```

Expected outcome:

- activation_score >= 90
- critical_failures = []
- starter-task spotlight + first-response SLA contract present
