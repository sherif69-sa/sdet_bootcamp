name: enterprise-compliance-lane
on:
  pull_request:
  workflow_dispatch:

jobs:
  enterprise-compliance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install -r requirements-test.txt -e .
      - run: python -m sdetkit enterprise-use-case --format json --strict
      - run: python -m sdetkit enterprise-use-case --execute --evidence-dir docs/artifacts/name13-enterprise-pack/evidence --format json --strict
      - run: python scripts/check_name13_enterprise_use_case_contract.py
