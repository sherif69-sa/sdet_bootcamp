# Ops Control Plane

`sdetkit ops init` bootstraps `.sdetkit/` with config, policies, playbooks, cache, and out folders.

- `sdetkit ops plan --profile ci`
- `sdetkit ops run --profile ci --jobs 2 --fail-fast`

By default commands are offline-first and deterministic.
